# Terraform configuration file for mROS 2.0

# Networking settings
network = "private"
subnets = ["10.0.0.0/24"]
region = "us-west-2"

# Security settings
key_name = "mros2_key"
ssh_cidr_blocks = ["0.0.0.0/0"]
allowed_ports = [22, 80, 443]

# Instance settings
instance_type = "t2.micro"
ami = "ami-0c2b8ca1dad447f8a"
user_data = ""

